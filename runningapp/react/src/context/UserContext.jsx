import { createContext } from "react";

/**
 * helper calss to hold users data through the app
 * id and other parameters
 * saved lots of time typing in irrelevant code
 */
export const UserContext = createContext(null);